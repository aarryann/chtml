export function <!--{ractkey}-->(data: Array<any>, decorator: Object, doc: Document = document) {
  <!--{ractkey}-->AddData(data, decorator, doc);
}

function <!--{ractkey}-->AddData(data: Array<any>, decorator: any, doc: Document = document) {
  const domDocument = doc || document;
  const container = domDocument.querySelector('[data-ract-container="<!--{ractkey}-->"]');
  const template = <HTMLTemplateElement>domDocument.querySelector(
    'template[data-ract-template="<!--{ractkey}-->"]'
  );

  if (!container || !template) return;
  let clone, rootNode;

  data.forEach((row) => {
    clone = <HTMLElement>template.content.cloneNode(true);
    rootNode = clone.children[0];
    let 
    <!--{#each items}-->
      <!--{this}--> = row["<!--{this}-->"],
    <!--{/each}-->
    dummy = true;

    <!--{setterCode}-->

    container.appendChild(rootNode);
  });
}
