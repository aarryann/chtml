export function <!--{ractkey}-->(data: Array<any>, decorator: Object) {
  <!--{ractkey}-->AddData(data, decorator);
}

function <!--{ractkey}-->AddData(data: Array<any>, decorator: any) {
  const container = document.querySelector('[data-ract-container="<!--{ractkey}-->"]');
  const template = <HTMLTemplateElement>document.querySelector(
    'template[data-ract-template="<!--{ractkey}-->"]'
  );

  if (!container || !template) return;
  let clone, rootNode;

  data.forEach((row) => {
    clone = <HTMLElement>template.content.cloneNode(true);
    rootNode = clone.children[0];
    let 
    <!--{#each items}-->
      <!--{this}--> = row["<!--{this}-->"],
    <!--{/each}-->
    dummy = true;

    <!--{setterCode}-->

    container.appendChild(rootNode);
  });
}
